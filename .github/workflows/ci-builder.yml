name: CI - Builder (jsonjsdb-builder)

on:
  push:
    branches: [main]
    paths:
      - 'jsonjsdb-builder/**'
      - '.github/workflows/ci-builder.yml'
      - '!**/*.md'
  pull_request:
    branches: [main]
    paths:
      - 'jsonjsdb-builder/**'
      - '.github/workflows/ci-builder.yml'
      - '!**/*.md'

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jsonjsdb-builder
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: 'jsonjsdb-builder/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Build package
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: jsonjsdb-builder-dist
          path: jsonjsdb-builder/dist/
          retention-days: 7

      - name: Check package integrity
        run: npm pack --dry-run
